import main
import discord
import requests

intents = discord.Intents.default()
discord.message_content = True
client = discord.Client(intents=varG__A = np.arange(Start, ExclusiveEnd, Step, dtype=int)  # import numpy as np
@client.event
async def on_message(message):
    print(main.on_message())